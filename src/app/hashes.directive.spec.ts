import { HashesDirective } from './hashes.directive';

describe('HashesDirective', () => {
  it('should create an instance', () => {
    const directive = new HashesDirective();
    expect(directive).toBeTruthy();
  });
});
